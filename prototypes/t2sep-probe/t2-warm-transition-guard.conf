# SPDX-License-Identifier: MIT
[Unit]
# Prevent D-Bus or another unit from bypassing `systemctl disable` and starting
# reset-capable services before the warm-transition evidence is consumed.
ConditionPathExists=/run/t2-touchid/allow-reset-capable-services

