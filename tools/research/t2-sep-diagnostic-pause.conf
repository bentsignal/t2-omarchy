# Temporary pause after macOS boot shutdowns were reported, 2026-09-13.
# Preserve all driver/state files, but prevent automatic or explicit modprobe.
# Does not unload the current driver or revoke registered DMA.
blacklist t2_sep_transport
install t2_sep_transport /usr/bin/false
