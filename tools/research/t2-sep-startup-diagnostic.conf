# Temporary startup diagnostic. Remove after investigating the SEP timeout.
# Uses the pinned driver's existing read-only version-1 capability handshake.
# Effective only when the module next loads; never unload registered SEP DMA.
options t2_sep_transport probe_capabilities=1
